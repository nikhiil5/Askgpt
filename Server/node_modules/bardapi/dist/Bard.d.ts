export declare class BardAPI {
    sessionId: string;
    private readonly headers;
    private conversationalData;
    private _reqid;
    constructor({ sessionId }: {
        sessionId: string;
    });
    private get_bard_config;
    ask({ message, previousChoiceId, }: {
        message: string;
        previousChoiceId?: string;
    }): Promise<BardChatResponse>;
    reset(): void;
}
export interface BardChatResponse {
    response: string;
    conversationId: string;
    responseId: string;
    choiceId: string;
    otherChoices: {
        choiceId: string;
        message: string;
    }[];
}
